fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
python-dotenv>=1.0
pydantic>=2.9
python-docx>=1.2
requests>=2.32
anthropic>=0.120

# Postgres driver — installed by default so switching DATABASE_URL to a
# postgresql:// URL (e.g. Railway's Postgres addon) works with no extra
# install step. The app still runs on SQLite out of the box either way;
# this driver simply sits unused if DATABASE_URL stays sqlite://.
psycopg[binary]>=3.2

# TF-IDF regulatory-clause retrieval (app/rag/) — no model download needed,
# small pure-wheel install, safe to bundle by default. /regulation/search
# has no ImportError fallback, unlike risk_engine.py's internal RAG call.
scikit-learn>=1.4
